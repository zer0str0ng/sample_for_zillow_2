import { DBController } from './db.controller';
import { MockController } from './mock.controller';
import { ReportController } from './report.controller';

export default [DBController, MockController, ReportController];
